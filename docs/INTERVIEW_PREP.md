# Interview Preparation & System Design Notes

## Core Topics
1. **Asynchronous Architecture**: Celery background queues with Redis broker.
2. **Vector Indexing**: HNSW index configuration for embedding retrieval.
3. **Database Indexing**: B-Tree indices on frequently queried foreign keys (`job_id`, `candidate_id`).
